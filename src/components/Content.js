import React from "react";

const Content = () => (
  <div className="container">
    <div id="content" className="row hidden">
      {/* 这里可以继续拆分子组件，例如 NameCard, Resume, Portfolio 等 */}
    </div>
  </div>
);

export default Content;